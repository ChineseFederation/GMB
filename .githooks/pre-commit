#!/usr/bin/env bash
# 本仓库的 pre-commit hook。启用方法：
#   git config core.hooksPath .githooks
set -e
REPO_ROOT="$(git rev-parse --show-toplevel)"

# chainspec.json 冻结守卫（bootNodes 变更不受限，仅校验创世内容）
"$REPO_ROOT/citizenapp/scripts/check-chainspec-frozen.sh"
