name: 公民 · 发布 · Android
run-name: 公民 · 发布 · Android

on:
  workflow_dispatch:
    inputs:
      source_sha:
        description: 正式 Release 准确提交
        required: true
        type: string
      version_tag:
        description: 公民 Android 正式版本 Tag
        required: true
        type: string

concurrency:
  group: citizenapp-publish-android
  cancel-in-progress: false

permissions:
  contents: read
  deployments: write

jobs:
  publish:
    name: 提交公民 Android 到 Google Play
    runs-on: ubuntu-24.04
    environment: citizenapp-android-production
    steps:
      - name: 拉取发布工具
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5.1.0

      - name: 校验发布依赖契约
        run: node .github/scripts/dependencies.mjs check --scope citizenapp

      - name: 锁定不可变正式 Release
        env:
          GH_TOKEN: ${{ github.token }}
          GMB_SOURCE_SHA: ${{ inputs.source_sha }}
          GMB_VERSION_TAG: ${{ inputs.version_tag }}
        run: |
          set -euo pipefail
          [[ "$GMB_SOURCE_SHA" =~ ^[0-9a-f]{40}$ ]]
          [[ "$GMB_VERSION_TAG" =~ ^citizenapp-android-v[0-9]+\.[0-9]{1,2}\.[0-9]{1,2}$ ]]
          test "$(gh release view "$GMB_VERSION_TAG" --json isDraft,isPrerelease --jq '[.isDraft,.isPrerelease] | @tsv')" = $'false\tfalse'
          git fetch --quiet origin "refs/tags/${GMB_VERSION_TAG}:refs/tags/${GMB_VERSION_TAG}"
          test "$(git rev-list -n 1 "$GMB_VERSION_TAG")" = "$GMB_SOURCE_SHA"
          mkdir -p "$RUNNER_TEMP/release"
          gh release download "$GMB_VERSION_TAG" --dir "$RUNNER_TEMP/release" \
            --pattern citizenapp.aab --pattern citizenapp-release-android.json

      - name: 在 GitHub Runner 提交 Google Play
        env:
          PLAY_KEY: ${{ secrets.PLAY_KEY }}
          GMB_SOURCE_SHA: ${{ inputs.source_sha }}
        run: >-
          node .github/scripts/mobile-store.mjs --platform android --product citizenapp
          --version-tag '${{ inputs.version_tag }}'
          --manifest "$RUNNER_TEMP/release/citizenapp-release-android.json"
          --asset "$RUNNER_TEMP/release/citizenapp.aab"
          --receipt "$RUNNER_TEMP/publish-receipt.json"

      - name: 在 GitHub 记录发布结果
        env:
          GH_TOKEN: ${{ github.token }}
        run: >-
          node .github/scripts/publish-record.mjs
          --receipt "$RUNNER_TEMP/publish-receipt.json"
          --environment citizenapp-android-production --description '公民 Android Google Play 发布'
          --url 'https://play.google.com/store/apps/details?id=com.crcfrcn.citizenapp'
