# 产品手册设计 QA

## 对比目标

- 视觉真源:
  `/Users/rhett/.codex/generated_images/019fa71f-b74d-7a23-beca-ae9d5a3522fa/call_B3zRcOJBHdZcYSTrx2nOwiK2.png`
- 实现截图:
  `/Users/rhett/.codex/visualizations/2026/07/28/019fa71f-b74d-7a23-beca-ae9d5a3522fa/product_manual_implementation.png`
- 同屏对比:
  `/Users/rhett/.codex/visualizations/2026/07/28/019fa71f-b74d-7a23-beca-ae9d5a3522fa/product_manual_comparison.png`
- 页面状态:深色主题、产品手册第 1 部分、页面初始滚动位置。
- 视口:390×867 逻辑像素。
- 视觉真源像素:844×1875,约 2.164 倍密度。
- 实现截图像素:390×867,1 倍密度;同屏对比时等比归一到 844×1875。

## 全屏对比证据

- 信息架构一致:顶部手册标题、密钥地图说明卡、五层交错密钥关系、中央单向派生线、
  保密/公开边界和底部恢复提示均完整出现。
- 版式方向一致:深色背景、蓝黑渐变卡片、冰蓝发光主线、金色机密状态、青色公开状态、
  左右交错卡片和圆角描边均继承选定方案。
- 实现使用公民钱包现有 `AppTheme` 色板和 Material 图标库,没有新增位图、手绘 SVG、
  占位图或设备外框。
- Flutter 测试渲染器不包含 Android/iOS 的中文与 Material Icons 系统回退字体,
  因此截图中的方框只属于测试截图环境;页面文字、图标与边界数量由 Widget 测试逐项
  校验，Android 的 `arm64-v8a` ABI 未签名 Release APK 构建同时验证平台字体与图标资源会被打包/解析。

## 聚焦区域对比

- 顶部说明卡:标题层级、分隔线、单向派生安全提示和卡片边界与真源一致,无需另做裁切。
- 密钥主线:五个节点顺序、左右方向、保密/公开颜色和 `//0 //1 //2 …` 派生提示均可在
  全屏同屏图中清楚核对,无需额外局部图。
- 底部提示:恢复能力与不能用地址恢复钱包的两句文案完整保留。

## 对比历史

### 第 1 轮

- [P2] 私钥卡片在 390 宽视口发生 21 像素纵向溢出,且整体密度比真源偏松。
- 修复:压缩说明卡间距、普通步骤卡高度、图标和正文尺寸;把私钥卡调整为独立高度,
  精简副标题但保留 `sr25519`、`AccountId`、硬派生与 SS58 展示语义。

### 第 2 轮

- 后续截图无 RenderFlex 溢出、重叠、裁切或不可达内容。
- 390×867 视口内五层关系与底部安全提示均保持可见,未发现可执行的 P0/P1/P2 差异。

## Findings

- 无 P0/P1/P2 finding。

## Follow-up Polish

- [P3] 真源使用定制线性图标和括号式边界装饰;实现按仓库既有设计系统使用 Material
  图标和文字边界标签。语义与可访问性更稳定,但线条个性略弱于概念图。
- [P3] 真源说明卡带更强的纹理光晕;实现使用现有 Flutter 渐变与阴影 token,视觉更克制。

## Implementation Checklist

- [x] 390 宽移动端布局无溢出。
- [x] 助记词、种子、私钥、公钥/AccountId、SS58 地址顺序准确。
- [x] 机密与公开边界同时用文字和颜色表达。
- [x] 设置入口与页面滚动内容可达。
- [x] Widget 测试验证关键文案和 3 个保密、2 个公开标签。

final result: passed

## 首页与账户页面定向调整（2026-08-11）

- 无钱包首屏使用全宽居中约束，钱包图标、标题、说明和两个按钮的水平中心必须与页面
  中心完全一致，不允许因滚动容器收缩宽度而靠左。
- 首屏采用上 20、下 92 的非对称内容留白，并在剩余高度内垂直居中；图标上方空白
  必须小于导入钱包按钮下方空白，小屏允许滚动且不得溢出。
- 首屏说明统一为“创建或导入钱包后开始使用”，旧文案不得保留。
- 顶部品牌图标固定以 22×22 直接显示 `shared/branding/logo.png` 唯一母版生成的完整青色米白
  “公”字圆角 Logo；页面不得另画渐变底、染色、放大裁切或维护第二份图形，不得恢复
  盾牌或黑色图形。`citizenwallet/assets/icons/citizen-logo.png` 只是统一生成物。
- 钱包详情“添加账户”固定为纯文字按钮；账户详情私钥副标题固定为
  “（只能自己悄悄看）”，字号、颜色和字重与公开信息副标题一致。
- Widget 测试需同时断言首屏四组核心元素的横向中心、上下留白关系、Logo 尺寸、纯文字
  添加账户按钮和私钥完整标题。iOS、Android 真机结果待依赖与构建生成物授权后补录。
