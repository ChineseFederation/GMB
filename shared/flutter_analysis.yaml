# 两款公民移动端共同的Flutter静态分析真源。产品根只保留自身排除项或额外规则，
# 禁止复制这组基础规则后形成不同的分析边界。
include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - "**/*.g.dart"

linter:
  rules:
    use_build_context_synchronously: true
