# CitizenSDK package tests

The `sdk/` child contains unit tests for the Android hardware-vault alias and
envelope contracts. Keeping them in the production package allows deliberate
testing of package-visible parsing helpers without widening the public API.

