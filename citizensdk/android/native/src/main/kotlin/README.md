# Kotlin sources

The `org.citizen.sdk` package is the stable Kotlin/Java facade. Its `internal`
subpackage is the private JNI, typed-store and hardware-vault boundary; its
`ui` subpackage owns all recovery-phrase input and display.

