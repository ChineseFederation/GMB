# Internal contract tests

Pure tests in this package cover exact operation admission, persistent record
keys and generation-scoped vault identity. SQLite and Keystore behavior remains
in `androidTest`.

