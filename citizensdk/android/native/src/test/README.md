# JVM contract tests

These tests cover the Java/Kotlin API shape and pure lifecycle, identity,
request-routing and record-key invariants. Android hardware guarantees are
covered by instrumentation on a qualifying physical device.

