# CitizenSDK 原生核心

该目录承载同一 CitizenSDK 产品的跨平台原生核心。最终只允许包含公民链轻节点、
sr25519、稳定 C ABI 及其必要的官方依赖适配。

当前包含 `signer` 基线、已经排除聊天/账户加密的 smoldot FFI 外层，以及逐字节迁入的
light-base 轻节点编排层。PoW 底层 `lib` 尚未迁入。后续必须保持单一 Rust 核心源码，
平台目录只能负责链接、装载和设备安全能力，不得复制五套链或签名实现。
