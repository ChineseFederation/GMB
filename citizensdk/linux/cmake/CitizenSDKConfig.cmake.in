@PACKAGE_INIT@

set(_CITIZENSDK_PACKAGE_PLATFORM "@CITIZENSDK_PLATFORM@")
if(DEFINED CITIZENSDK_PLATFORM AND
   NOT "${CITIZENSDK_PLATFORM}" STREQUAL "${_CITIZENSDK_PACKAGE_PLATFORM}")
  set(CitizenSDK_FOUND FALSE)
  set(CitizenSDK_NOT_FOUND_MESSAGE
      "CitizenSDK cannot mix LinuxARM and LinuxAMD targets in one CMake graph")
  return()
endif()
set(CITIZENSDK_PLATFORM "${_CITIZENSDK_PACKAGE_PLATFORM}")
set(CITIZENSDK_LIBRARY_DIR "@PACKAGE_CMAKE_INSTALL_LIBDIR@")
set(CITIZENSDK_ASSET_DIR "@PACKAGE_CMAKE_INSTALL_DATADIR@/citizensdk/citizenchain")

if(NOT TARGET CitizenSDK::Core)
  add_library(CitizenSDK::Core SHARED IMPORTED)
  set_target_properties(CitizenSDK::Core PROPERTIES
    IMPORTED_LOCATION "${CITIZENSDK_LIBRARY_DIR}/${CITIZENSDK_PLATFORM}/libcitizensdk.so"
    INTERFACE_INCLUDE_DIRECTORIES "@PACKAGE_CMAKE_INSTALL_INCLUDEDIR@"
  )
endif()

include("${CMAKE_CURRENT_LIST_DIR}/CitizenSDKDependencies.cmake")
include("${CMAKE_CURRENT_LIST_DIR}/CitizenSDKTargets.cmake")
check_required_components(CitizenSDK)
